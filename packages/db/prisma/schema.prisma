generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// =============================================================
// Entidades serão adicionadas na Sprint 1:
// - Agency, Tenant, User, Membership
// =============================================================
